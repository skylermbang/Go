package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Printf("Now you have %g problems.\n", math.Sqrt(7))
}
/* 2/17
 %g  to bring the arg 

3/17
 exported name  start with capitalized letter 

4/17
func add(x int, y int) int {}  the types comes after the variable name 
it can be  add(x, y int)


6/17
:= short assignment statement can be used in place of a var declaration with implicit type.
A return statement without arguments returns the named return values. This is known as a "naked" return

8/17
var statement declares a list of variables; as in function argument lists, the type is last.

<type>
/ int  , float64, boolean
